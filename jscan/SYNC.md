# pyscn → jscan sync management

Language-independent algorithms now live in `core/` and are consumed directly by both tools (jscan: Phase 2b, #9–#11; pyscn: Phase 3, [polyscan#12](https://github.com/ludo-technologies/polyscan/issues/12), completed 2026-07-23). Changes to those algorithms land in `core/` itself and need no porting between pyscn and jscan. This file now tracks only the **case-by-case** and **reference-only** areas — pyscn changes there are reviewed and selectively ported to jscan, manually or by an agent.
The `/sync-pyscn` command reads this file to run a sync.

- Upstream: https://github.com/ludo-technologies/pyscn (local clone: `../pyscn`)
- jscan's Go code lives at `polyscan/internal/js/` in this monorepo (the `internal/js` package of the `polyscan` module); the jscan paths in the tables below are relative to that directory. Its CLI surface is `polyscan analyze` only: there is no `check` command, so pyscn's `check`-gate changes have nothing to land on
- **Sync baseline SHA**: `7d2d90f6403bf86f649f73185e8856beff538960` (2026-09-26, v1.32.1+19)
  - The initial catch-up (clone detection / scoring / misc, 2026-06-11–12) compared jscan against pyscn's state up to `fb3fe92`. Exceptions that were deliberately not ported are listed under "Pending changes".
  - Update this value to pyscn's `origin/main` HEAD on each sync run.

## Sync policy classification

| Classification | Meaning |
|---|---|
| **sync** | Language-independent algorithm. Port essentially all pyscn changes |
| **case-by-case** | Shared concept but needs language adaptation. Decide per change whether to port |
| **reference-only** | Language-specific. Never port code. Only report significant design-direction changes to a human |

No row currently uses **sync**: every area that was language-independent enough to sync 1:1 has moved into `core/` and is consumed directly by both tools (see Sync history). The classification stays defined here in case a newly-shared, not-yet-core-extracted algorithm shows up again.

## File mapping

### internal/analyzer

| pyscn | jscan | Classification | Notes |
|---|---|---|---|
| `internal/analyzer/apted_tree.go` | `internal/analyzer/apted_tree.go` | reference-only | jscan retains only the JS/TS parser-to-`core/apted.TreeNode` converter |
| `internal/analyzer/apted_cost.go` | `internal/analyzer/apted_cost.go` | reference-only | Cost models are language-specific (Python AST vs JS/TS AST) |
| `internal/analyzer/lsh_index.go` | `internal/analyzer/lsh_index.go` | case-by-case | Thin candidate-index adapter (int fragment IDs, deterministic order, `WithMaxCandidates` cap) kept local in both tools around `core/lsh`; not itself in core, so still worth diffing when either side changes it |
| `internal/analyzer/clone_detector.go` | `internal/analyzer/clone_detector.go` | case-by-case | Pipeline orchestration + fragment extraction stay language-side; grouping/dedup/gates/classifier come from `core/clone`. Splitting groups along the retained pairs is local in both tools until [polyscan#173](https://github.com/ludo-technologies/polyscan/issues/173) moves it to core: pyscn has `splitDisconnectedGroups` here and `filterCloneGroupsByPairs` in `service/clone_group_filter.go`, and jscan has one `SplitGroupsByPairs` in `internal/analyzer/clone_group_split.go` that its detector and clone service both call |
| `internal/analyzer/cfg.go` | `internal/analyzer/cfg.go` | case-by-case | Both tools alias `core/cfg` types for internal source compatibility; rarely needs edits unless `core/cfg`'s public shape changes |
| `internal/analyzer/cfg_builder.go` | `internal/analyzer/cfg_builder.go` | reference-only | Control-flow semantics are language-specific (try/except/match vs try/catch/switch/hoisting) |
| `internal/analyzer/reachability.go` | `internal/analyzer/reachability.go` | case-by-case | Both tools retain a thin compatibility result projection over `core/cfg` reachability |
| `internal/analyzer/complexity.go` | `internal/analyzer/complexity.go` | case-by-case | JS contributes logical and ternary operators and enriches the shared result |
| `internal/analyzer/dead_code.go` | `internal/analyzer/dead_code.go` | case-by-case | JS retains line/snippet/reason enrichment and unused-code analysis |
| `internal/analyzer/cbo.go` | `internal/analyzer/cbo.go` | reference-only | AST traversal for dependency collection is language-specific |
| `internal/analyzer/coupling_metrics.go` | `internal/analyzer/coupling_metrics.go` | case-by-case | Both tools delegate Martin metrics/Tarjan-adjacent coupling math to `core/graph`, supplying their own abstractness signal and risk/zone summaries. Both compute max depth over the load-time graph (lazy/dynamic edges excluded) while keeping every edge for the Martin metrics themselves; both read depth and chains off one `core/graph.ChainFinder` over their load-time graph (pyscn adopted it in `693c8ac`, retiring its local `dependency_topology.go` condensation) |
| `internal/analyzer/circular_detector.go` | `internal/analyzer/circular_detector.go` | case-by-case | Both tools delegate Tarjan SCC to `core/graph`; each filters dynamic/lazy imports and enriches cycle results independently |
| `internal/analyzer/dependency_graph.go` | `internal/analyzer/dependency_graph.go` | case-by-case | Graph construction is shared; ModuleInfo contents are language-specific |
| `internal/analyzer/textual_similarity.go` | *(adopted `core/clone`)* | case-by-case | Type-1 gate in `core/clone.TextualSimilarityAnalyzer`; jscan injects `removeJSComments` as `CommentStripper` (`javascript_comments.go`) |
| `internal/analyzer/module_analyzer.go` | `internal/analyzer/module_analyzer.go` | reference-only | Import resolution is language-specific (`__init__.py` vs ESM/CJS/Node builtins) |

### service

| pyscn | jscan | Classification | Notes |
|---|---|---|---|
| `service/project_snapshot.go` | `service/project_snapshot.go` | case-by-case | Same design (parse once in parallel, path-ordered, lazily shared CFGs via `sync.Once`), ported in [polyscan#37](https://github.com/ludo-technologies/polyscan/issues/37). Divergences: jscan keeps `Content` (line counts, clone Type-1 source, unused-import type-only lines) instead of pyscn's `RawMetrics` cache; jscan has no `ProjectSnapshotOptions`; jscan's snapshot entry points validate `req.Paths` against the snapshot instead of ignoring it; single-analysis runs stream per-file `ProjectFile`s through the same analysis code (`analyzeProjectFilesFromPaths`) instead of pyscn's separate parse-inline `Analyze` bodies, so a lone analysis never holds every parse tree at once |
| `service/*_formatter.go`, `service/analyze_html.go`, `service/html_template.go`, `service/templates/` | `service/output_formatter.go`, `service/html_formatter.go`, `service/directory_complexity_formatter.go` | reference-only | The whole output layer — text, HTML — renders each tool's own result types and dimension set, so nothing ports as code. pyscn's report shows LCOM, communities, suggestions and cognitive complexity, none of which jscan has; the tab sets differ (pyscn 5 since #717, jscan 7); both templates are tool-specific Go string constants, with pyscn's `analyze` report since extracted into embedded `templates/analyze/report.{html,css,js}`. **The one thing that does stay aligned is the machine-readable output**, tracked on the `domain/output.go` row: the analyze JSON schema is a compatibility surface, its rendering is not. Report a redesign to a human and let each tool answer it natively — pyscn #717 became [polyscan#65](https://github.com/ludo-technologies/polyscan/issues/65) this way. Because the JSON schema is already shared, a single renderer in `core/` is a live option rather than a permanent divergence; that is a separate design call, not a sync decision |

### domain (scoring, type definitions)

| pyscn | jscan | Classification | Notes |
|---|---|---|---|
| `domain/analyze.go` | `domain/analyze.go` | case-by-case | Health-score summation, normalization, and grade mapping (`GradeFromScore`, `IsHealthyScore`) now come from `core/domain` in both tools — each has its own parity test against core (jscan: `analyze_test.go`; pyscn: `TestGradeParityWithCore`), so grade computation is identical by construction. `calculateComplexityPenalty` (jscan: high/medium count ratio, ESLint-aligned; pyscn: max of avg-McCabe/cognitive/nesting) and `calculateDeadCodePenalty` (jscan: per-file rate; pyscn: log-normalized) stay intentionally divergent — do not overwrite |
| `domain/system_analysis.go` | `domain/system_analysis.go` | case-by-case | Same as above |
| `domain/complexity.go` | `domain/complexity.go` | case-by-case | Keep thresholds and risk-level definitions aligned. Both tools compute every `ComplexitySummary` aggregate over the complete analyzed population and carry `TotalFiles`/`SkippedFiles` for the parse-error disclosure |
| `domain/clone.go` | `domain/clone.go` | case-by-case | Keep similarity thresholds and clone-type definitions aligned |
| `domain/cbo.go` | `domain/cbo.go` | case-by-case | Both tools keep type-annotation references in the `type_hint_dependencies` breakdown only, out of `coupling_count` and the risk level (pyscn #757 `1272336`; jscan 2026-09-15 sync). jscan's CBO unit is the file, so `base_classes` is always empty and there is no `most_depended_upon_classes` |
| `domain/dead_code.go` | `domain/dead_code.go` | case-by-case | |
| `domain/output.go` | `domain/output.go` | case-by-case | Mind JSON output schema compatibility |
| `domain/errors.go` | `domain/errors.go` | case-by-case | |
| `domain/analysis_coverage.go` | `domain/analysis_coverage.go` | case-by-case | Both tools type skipped files as `AnalysisDiagnostic` (`read_error` / `parse_error`), count coverage once in `ProjectSnapshot.Coverage()`, and emit a top-level `diagnostics` array in the analyze JSON (pyscn `e68a7c8`; jscan [polyscan#140](https://github.com/ludo-technologies/polyscan/issues/140), 2026-09-16). jscan deliberately has no `AnalysisFailure` / `failures`: keeping partial results when an analyzer fails on one file is graceful degradation, which FailFast rules out, so an analyzer failure stays an error of that analysis. jscan also has no `AnalysisKind` or `service/analysis_issues.go`; `ProjectFile.Diagnostic()` is the one classification point |

### Claude Code plugin / Agent Skills

| pyscn | jscan | Classification | Notes |
|---|---|---|---|
| `.claude-plugin/marketplace.json` | `.claude-plugin/marketplace.json` | case-by-case | Marketplace/plugin naming differs (`pyscn-mcp` vs `jscan`) |
| `.claude-plugin/pyscn-mcp/.claude-plugin/plugin.json` | `.claude-plugin/jscan/.claude-plugin/plugin.json` | case-by-case | jscan has no MCP server, so the plugin is skills-only (no `mcpServers`) |
| `.claude-plugin/pyscn-mcp/skills/*/SKILL.md` | `.claude-plugin/jscan/skills/*/SKILL.md` | case-by-case | Same 4 skills (health-check / cli-analysis / refactoring / architecture-review). Content rewritten for jscan's CLI: `npx jscan@latest`, `--select clone` (singular), `--text`/`--json` to stdout (no `.pyscn/reports/`-style report dir), no LCOM/communities/mockdata/di, and the "MCP Alternative" sections dropped until jscan has an MCP server |

### jscan-specific (no upstream)

- `internal/analyzer/unused_code.go` — cross-file dead code detection (includes the Next.js App Router exception conventions)

### pyscn-specific, unported features (future porting candidates)

Out of sync scope, but reference points when considering porting features to jscan:

- Cognitive complexity: `cognitive_complexity.go`, `raw_metrics.go`. (`nesting_depth.go` no longer belongs here: jscan measures nesting depth its own way in `internal/analyzer/complexity.go`, see "Ported with divergences")
- LCOM4: `lcom.go`, `domain/lcom.go` — the union-find (shared-variable + call-edge unions) and risk assessment now live in `core/lcom.ComputeLCOM4` / `AssessRisk` (pyscn Phase 3, PR #673); adopting this in jscan means pulling from `core/lcom` directly, not porting pyscn's original implementation. Python method/attribute extraction (decorator exclusion, `@property` reclassification, ctypes `_fields_`) stays pyscn-local
- DFA (unused-variable detection): `dfa.go`, `dfa_builder.go` — def-use chain construction and reaching-def linking now live in `core/dfa` (pyscn Phase 3, PR #673) via the `RefExtractor`/`ParamExtractor` extension points; Python def/use extraction stays pyscn-local
- DI anti-pattern detection: `di_antipattern_detector.go` plus `di_*.go`, `*_detector.go`, `framework_patterns.go`
- Split similarity-analysis structure (remaining): `structural_similarity.go`, `semantic_similarity.go`, `similarity_analyzer.go`, multi-dimensional `clone_classifier` — Type-1/2 gates are in `core/clone` (jscan adopted); semantic evidence penalties (disjoint literals, missing strong signals, incompatible return categories) now live in `core/semantic.ApplySemanticEvidence` (pyscn Phase 3, PR #673); structural similarity still fully unported
- Re-export resolution: `reexport_resolver.go`
- Improvement suggestions: `domain/suggestion.go`
- MCP server: `mcp/`, `cmd/pyscn-mcp/`
*(Module / directory quality rollups were on this list until [polyscan#31](https://github.com/ludo-technologies/polyscan/issues/31); see "Ported with divergences" below.)*

### Ported with divergences

- **Module / directory quality rollups** (pyscn #686 + #533/#687, ported for jscan in polyscan#31) — jscan has `domain/module_quality.go`, `domain/directory_complexity.go`, `service/module_quality.go`, `service/directory_complexity_formatter.go`, plus `ComplexityResponse.ByDirectory` / `ModuleRollups`, `DeadCodeResponse.ModuleRollups`, and a `module_quality` array on the analyze JSON/YAML output. JSON field names match pyscn. The divergences, all deliberate:
  - **No `average_cognitive_complexity`** — cognitive complexity is still unported (see the list above). The field is absent rather than reported as zero
  - **No `function_count`** — in pyscn it is the module metadata's function count next to the complexity records' `analyzed_function_count`. jscan's complexity analysis records every function it parses, so the two counts are the same number and only one is reported
  - **`lines_of_code` comes from the complexity service**, not from module metadata: it is the only per-file reader that already holds the file content. The dependency analysis measures no sizes at all: `ModuleDependencyMetrics` carried size fields that nothing ever filled, and [polyscan#144](https://github.com/ludo-technologies/polyscan/issues/144) removed them rather than keep serializing zeros. `module_quality` therefore reads only `ModuleName` from it. Line counting matches pyscn's `countSourceLines`
  - **The aggregation lives in the service layer, not the use case** — jscan's `analyze` command calls the services directly, so the use-case seam pyscn joins at does not sit on jscan's main path. `BuildModuleQuality` is a `service` function next to `BuildAnalyzeSummary`, which the analyze command already shares
  - **The directory root is the analyzed files' common ancestor**, not the caller-facing path set: the services receive collected file lists, not the caller's paths. Reported paths therefore strip the shared prefix of the selection, and a single-directory run reports one `.` row
  - **HTML tables are not sortable** — jscan's report has no table sorting anywhere, and adding it to only these two tables would be inconsistent
  - **`nesting_depth` was implemented as part of the port.** `CalculateNestingDepth` existed but was never called and counted control structures instead of measuring depth, so the published per-function `nesting_depth` was always 0. It now measures real depth (else-if chains stay flat, a catch clause stays at its try's level, nested functions are measured separately) and the directory rollups aggregate it
- Module community detection: `internal/analyzer/community_metrics.go`, `domain/community.go`, `app/community_usecase.go`, `service/community_analysis_service.go`, `service/community_formatter.go`, `service/community_config_loader.go` — new pyscn feature (2026-06-22–30, #582/#583/#602) clustering modules into communities (modularity, cross-community edges, bridge modules, package/layer alignment) with an opt-in Health Score penalty (`MaxCommunityPenalty`, max 10). Entirely new subsystem with CLI/config/MCP wiring across ~15 files; not present in jscan at all. The Health Score integration in `domain/analyze.go` only activates when `CommunitiesEnabled` and `CommunityCount >= 2`, so it has no effect on jscan's (unmodified) grade computation as long as the feature stays unported

## Pending changes

(Record changes here that a sync run decided not to port this time.)

Skipped during the clone detection catch-up (2026-06-11):

- **Docstring skipping** (`SkipDocstrings` and the related fields in `apted_tree.go` / `clone_detector.go` / `domain/clone.go`) — Python-specific. JS/TS ASTs have no docstring statements
- **Boilerplate cost model** (`NewPythonCostModelWithBoilerplateConfig`, `ReduceBoilerplateSimilarity` / `BoilerplateMultiplier`) — depends on Python framework patterns such as dataclass/Pydantic (`framework_patterns.go` is an unported feature)
- **Multi-dimensional classifier and DFA** (`CloneClassifier`, `EnableMultiDimensionalAnalysis` / `EnableSemanticAnalysis` / `EnableDFA`) — depend on pyscn-specific unported features (semantic/structural similarity, DFA)
- **Type-4 CFG gating** (`87babcc`, `9efebd4`) — changes inside `semantic_similarity.go` (unported). Port together with the semantic analysis
- **`CloneConfigurationLoader.MergeConfig`** — config-loader-layer change. jscan's loader implementation differs, so out of scope
- **star_medoid graph optimization** (`buildSimilarityGraph` / `mostSimilarMedoid`) — performance-only change, structurally different from jscan's StarMedoid implementation (iterative reassignment over domain.Clone). The `averageGroupSimilarity` change to count only existing pairs was ported
- **pyscn's removal of the content-less `ExtractFragments`** — jscan tests still use it, so both variants were kept

Skipped during the scoring catch-up (2026-06-11):

- **LCOM cohesion scoring** (`CohesionMediumWeight` / `CohesionSaturationRatio`, `calculateCohesionPenalty`, `CohesionScore`, the LCOM summary fields, `57fcc66`) — depends on the unported LCOM4 feature. Port together with `lcom.go` / `domain/lcom.go`
- **Cognitive complexity / RawMetrics fields in `domain/complexity.go`** (`8aa6d21`, `ce469ce`) — unported features
- **Mock/synthetic fixture detection** (`MockData*` fields, `15bd414`) — pyscn-specific lint feature
- **Suggestions** (`Suggestions` field, `1eaf9f6`) — depends on unported `domain/suggestion.go`
- **Analyze config plumbing** (`AnalyzeOutputFormatter`, `AnalyzeExecutionConfig`, `AnalyzeConfigurationLoader`, `Enabled`/`ReportUnchanged`/`Request` fields, `222d190`, `779200a`, `13644b4`, `e79a414`, `d6fd3da`, `ca75d12`) — jscan's config-loader layer differs, out of scope
- **Architecture style presets / warn rules / neutral prefixes / cohesion-responsibility validation** (`ad79460`, `1c9839d`, `61f0431`, `3708845`, `ddcf1c5`) — jscan does not implement architecture validation (the `ArchitectureAnalysisResult` types are unused scaffolding); only the `WeightedViolations` field was ported for type alignment
- **`AbstractClassCount`** (`f63540d`) — Python ABC detection; jscan computes abstractness its own way in `coupling_metrics.go` from TS interfaces/abstract classes
- **Django migration exclusion / Python default patterns** (`a33f1c7`, `DefaultPythonModuleIncludePatterns`) — Python-specific

Skipped during the misc catch-up (2026-06-12):

- **APTED DP-matrix reuse + parallel pair verification** (`c324d2f`, `e912ca2`) — landed in pyscn after the clone-detection catch-up point (`73acc60`). Large performance rework of `apted.go` / `apted_tree.go` / `clone_detector.go` (worker pool, `newWorkerDetector` / `effectiveWorkers`, key-root cache invalidation, streamed LSH verification). Port as its own dedicated change; the LSH int-ID/`WithMaxCandidates` part was already ported in the misc catch-up
- **complexity.go AST-based statement metrics + CognitiveComplexity** — the fix targets Python `match` decision-point double-counting (jscan counts JS branches its own way via logical/ternary operators), and `CalculateCognitiveComplexity` is an unported feature. Port together with `cognitive_complexity.go` if ever
- **cfg.go `ModuleNode` / `complexitySourceNode`** — unnecessary in jscan: the module-level CFG already sets `FunctionNode` to the Program node, so location/nesting info is available
- **`domain/defaults.go` constants refactor** (`DefaultAnalysisIncludePatterns`, `DefaultCBOLowThreshold` etc. in `domain/cbo.go` / `domain/dead_code.go`) — config-layer cosmetics with Python file patterns; jscan keeps its own defaults
- **`DeadCodeResponse.Request` field** — analyze-config plumbing (already skipped as a category during the scoring catch-up)

Skipped during the 2026-07-08 sync (`fb3fe92`..`249b121`):

- **`refactor(clone): integrate statistics into detector result types`** (`45b2058`) — pure internal Go API refactor (`DetectClones*` now returns a `*CloneDetectionResult` struct instead of a `([]*ClonePair, []*CloneGroup)` tuple); no algorithmic or output change. jscan's `CloneDetector`/service-layer split is already architecturally different (jscan builds `domain.Clone` directly via `fragmentToClone`, no separate service-layer conversion step), so there's nothing equivalent to restructure
- **CBO nested-class-scope exclusion** (`441ddc2`, `8b228b4`, issue #547) — excludes a locally/class-body-scoped nested class from CBO coupling counts, with Python-scoping-specific resolution (function-local vs. class-body visibility rules, method-signature-annotation evaluation scope). `cbo.go` is reference-only (AST traversal is language-specific); the underlying idea (don't count a helper class/function scoped inside the analyzed class as coupling) may be worth an independent JS/TS-native implementation later, but the resolution logic itself is not portable
- **CBO enum-member collapsing** (`67b243f`) — collapses `EnumClass.MEMBER`-style attribute reads into one coupling edge per imported class, keyed off Python's `import ... as ...` dotted-name resolution (`isImportedDependency`, `looksLikeClassReference`), which jscan's `cbo.go` has no equivalent of
- **CBO namespace-import grouping** (`06c76a4`, issue #517) — `GroupNamespaceImports` option collapsing `alias.Member` references for `import mod as alias`; Python-import-specific, with config plumbing through `domain.CBORequest`/service/config-loader layers that don't map onto jscan's config system
- **Cognitive-metrics-based risk / Health Score integration** (`3f159b4`) — makes `calculateComplexityPenalty` also weigh `AverageCognitiveComplexity` and `AverageNestingDepth` (unported features; see "pyscn-specific, unported features"). `calculateComplexityPenalty` is also flagged in the file mapping as an intentional jscan divergence (ESLint-aligned high/medium ratio vs. pyscn's average-complexity curve) — do not overwrite
- **Module community detection** (`9bc832e`, `1b0590b`, `785e72b`, `8b03fbb`, `fd0db46`, `7a63058`, #582/#583/#602) — new pyscn subsystem; see "pyscn-specific, unported features" for details

Skipped during the 2026-07-25 sync (`249b121`..`6af3ee7`):

- **CBO Python-scoping/import refinements** (`0a4c469`/#637 same-file top-level peer exclusion, `28d1378` name-collision hardening, `acbd2c8` module-constant exclusion, `826491b` PEP 563 annotation-only deps, `ff01735`/#644 Cython primitives, `c3edc9e` module-qualified enum coupling, `0238bab` acronym constructor coupling) — `cbo.go` is reference-only. Design direction worth noting: pyscn keeps tightening CBO noise exclusion; the *concepts* of same-file peer-class exclusion and module-constant exclusion may deserve an independent JS/TS-native implementation later
- **Sparse request bool overrides / sentinel unification** (`6a12c8b`, `f1e9a4b`, `a70b5a5`) — pyscn config-loader-layer rework (`FlagTracker` removal, `BoolPtr` sparse requests, `ShowDetails` response resolution). jscan's config-loader layer differs; already-skipped category (analyze-config plumbing)
- **LCOM4 regression test** (`21e512e`) — LCOM is an unported feature

Skipped during the 2026-07-28 sync (`6af3ee7`..`81b7ec0`):

- **Architecture-suggestion refinements** (`3e7d83c`/#647 dedup + `file_path`/`start_line`, `a146222` preserve distinct violations, `ab8f944` prioritize critical suggestions, `0edce40` identity-dimension tests, `5554baf` per-problem violation merge + shared category cap) — all in `domain/suggestion.go`, which is on the unported-features list ("Improvement suggestions"). jscan has no suggestion subsystem to apply these to; port them together with `domain/suggestion.go` if it is ever adopted
- **Module quality rollups** (`91075ec`, `270eb98`, `ca0c48b`, `ed9602c`, `499a05f`, `5e193e9`, `8c32d21`, `db2de9b`, `6a51bc8`, `0043feb`, plus `fe247fa`/#686 and the unmapped app/service/report commits `3093066`, `77af996`, `52ddb7f`, `9f60bd7`, `9394e7c`, `72cde27`, `df7803f`, `f2b7a3d`, `9487428`, `4c26413`) — new pyscn subsystem; skipped by the sync run and **ported afterwards in [polyscan#31](https://github.com/ludo-technologies/polyscan/issues/31)**, see "Ported with divergences"
- **Directory quality rollups** (`d985d0f`, `21c8636`, `f0713b7`, `3308c94`, `b6e5e3c`, `be35e03`, `6209e60`, plus the unmapped `39ca5ec`, `5b7226f`, `535c8e6`, `72337d2`, `af33f81`, `d9d8c64`, `ce8d982`, `d39ddef`, `86e5875`, `86d3ed2`, `3dd97bf`, `40dd367` — #533/#687) — second half of the same feature; same tracking issue. Note `d9d8c64` (buffer the analyze text report and propagate a single write error) is a genuinely portable formatter fix, but it exists only to wrap the new directory/module sections — jscan's `AnalyzeFormatter` writes with `fmt.Fprint` throughout and has no equivalent error-propagation seam to fix in isolation. The rollups themselves were **ported afterwards in [polyscan#31](https://github.com/ludo-technologies/polyscan/issues/31)**
- **MCP registry listing** (`300e1c9`, `9c3ab20`) — jscan has no MCP server

Skipped during the 2026-08-14 sync (`81b7ec0`..`1ab776e`):

- **Per-function SLOC and long-function warnings** (`01d98f7`/#551, `1f36711`, `880b7b1`) — pyscn derives per-function SLOC from `raw_metrics.go`'s line classification (an unported feature) via a per-line prefix sum, and plumbs the warn/critical thresholds through its config-loader layer (an already-skipped category). Genuine future candidate for jscan: the complexity service already reads file content for its `lines_of_code` rollup, so a JS/TS line classifier is the only missing piece
- **Cognitive complexity scope boundaries** (`a329355`/#658, `867e789`) — `cognitive_complexity.go` is unported
- **Config discovery from the analyzed target** (`f818f22`/#666) — config-loader-layer rework (`LoadDefaultConfig(targetPath)`, a shared `discoverConfigFile`). jscan's `loadCommandConfig` already resolves configuration from the target path, so there is nothing to fix
- **Dependency topology subsystem** (`701602d`, `b9da321`, `690791b`, `3abc47b`, `6838d1c`, `67a7dc5`, `23cdf70`, `d7583f5`, `423a2c9`, `982c6ed`, `55341d1`, `c035f9d`, `58b856b`, `604429c`, `9256cd5`, `7ba0ac1`, `607a740`, `bedbdcc`, `0197d1f`, `47ffc88`) — pyscn built `internal/analyzer/dependency_topology.go` + `dependency_chains.go`: an SCC condensation shared by max depth and ranked chains, made linear, cancellable and immutable. This is convergent evolution with `core/graph.ChainFinder`, which jscan already consumes (polyscan#36), so there is nothing to port. It belongs in `core/` instead — see the core issues below. Only the load-time semantics (`29115cb`) were portable and were ported

Skipped during the 2026-08-17 sync (`1ab776e`..`f4f569c`):

- **Gate the analyzed population, not the reported functions** (`64a6876`) — pyscn's `check` iterated the display-filtered function list, so `report_unchanged = false` hid a complexity-1 but 123-SLOC function from the **function-length** gate; the fix adds an unserialized `AnalyzedFunctions` field to the response and gates over it. Nothing to land on: polyscan has no `check` command (and no `--max-complexity` or `--allow-parse-errors` flag), and per-function SLOC is unported (see "pyscn-specific, unported features"). Revisit if a quality gate command is ever added
- **Analyze HTML report redesign** (`6662eb7`, `fe8e24b`, #717) — output layer, reference-only, reported to the human instead (this run also added the output-layer row to the `service` mapping, which until now was reference-only by omission): pyscn moved its report template out of the Go string constant into embedded `report.html`/`report.css`/`report.js`, rebuilt the Overview around a verdict, per-dimension score cards, top suggestions, hotspot files and a complexity histogram, and merged 12 tabs into 5. jscan's report is still an 864-line Go string template in `service/html_formatter.go` with its own tab set, so none of it ports as code. Tracked for jscan as [polyscan#65](https://github.com/ludo-technologies/polyscan/issues/65)

Already in sync, nothing to port (`1ab776e`..`f4f569c`): pyscn's core adoptions of the chain finder (`693c8ac`, [polyscan#56](https://github.com/ludo-technologies/polyscan/issues/56)) and the parse-error penalty (`eb372cb`, [polyscan#57](https://github.com/ludo-technologies/polyscan/issues/57)) both land jscan's existing behavior on pyscn's side; deterministic class-ranking tie-breaks (`74f2512`) already exist in jscan's `sortClasses`/`generateSummary` as `classPrecedes`/`couplingPrecedes`, and its LCOM half is an unported feature; the Japanese-comment translation (`fe766fe`) has no counterpart, as jscan carries no Japanese comments.

Found while verifying the load-time port, not a pyscn change: **jscan's module analyzer does not detect `import()` at all** — `processDynamicImport` never fires, so no dynamic edge is ever built, and both the load-time depth exclusion and the older cycle-detection exclusion (#460) are currently unreachable end to end. `internal/analyzer/dependency_graph_test.go` already skips a test over this ("Dynamic imports not detected by parser"). Worth its own fix ([polyscan#58](https://github.com/ludo-technologies/polyscan/issues/58)).

Skipped during the 2026-09-15 sync (`f4f569c`..`7358893`, v1.29.1+10..v1.32.0):

- **Class scopes** (34 commits: `a7266fc`, `e539801`, `b990856`, `9b622c4`, `c4b0e0e`, `26802b9`, `f01b092`, `25ba99b`, `7ed1b07`, `80ac979`, `de72b20`, `d41c932`, `1e1c173`, `5ef0242`, `88d6587`, `bb5f529`, `ad77359`, `664ffc8`, `77c88a2`, `352476a`, `135b8e8`, `9125033`, `d8abdf6`, `958dd24`, `2f744af`, `c8f7a7d`, `ac76b10`, `bc7b428`, `d61ffcd`, `7c3d1f7`, `d8706d7`, `38072c4`, `15415ec`, `9c3e751`, `fb6c225`) — pyscn now builds a typed CFG for each Python class body, which executes as a statement suite when the `class` statement runs, and reports complexity and dead code for it (`AnalysisScopeKind` module/function/class, `ClassScopes` collections, `TotalClassScopes` / `MaxClassComplexity` / `HighRiskClassScopes` summary fields). Python-semantic: a JS/TS class body is a list of member declarations, not an executed suite, and `cfg_builder.go` is reference-only. pyscn deliberately kept the numeric health score free of the class metrics (`26802b9`), so nothing in scoring diverges. ES2022 `static {}` blocks are the only JS/TS analogue and were not investigated; a narrow follow-up at most
- **ProjectSnapshot / module-graph restructuring** (36 commits: `5f794c7`, `8c876a7`, `414d95d`, `c57b9f3`, `55a2b8b`, `a7ef2e6`, `cd92dac`, `f960ecf`, `0031b72`, `f2ffd97`, `60d93c4`, `203e22c`, `f09acb0`, `a8db7d2`, `4356e60`, `500bfa0`, `7eb5730`, `fb77377`, `27c980f`, `20374ee`, `496cc7c`, `b70124e`, `7ac2a55`, `d447d40`, `9c48992`, `860466b`, `ec3172c`, `1a66fc2`, `610d3e7`, `e5a6e91`, `29199b1`, `e40bea6`, `585407a`, `3d2e8da`, `46a6f84`, `bc99c1b`) — seals the snapshot's private state, captures Python module roots once instead of rediscovering them from disk, and moves dependency-graph construction into the service layer as a `ProjectModuleGraph` owned by the snapshot. No externally visible effect; jscan's snapshot is documented above as intentionally divergent and its module analyzer never touched disk
- **Typed analysis diagnostics / centralized coverage** (`e68a7c8`, `b39fdf1`, `c418238`, `1149d1b`, `f4b3afb`, `f01cde2`, `9858a94`, `f60fdb0`, `6964a1e`, `73a7c96`) — new `domain/analysis_coverage.go` (`AnalysisDiagnostic`, `AnalysisFailure`, `AnalysisCoverage`), `service/analysis_issues.go`, and `ProjectSnapshot.Coverage()` computing file counts once for every service. The diagnostics and `Coverage()` half was ported on 2026-09-16 as [polyscan#140](https://github.com/ludo-technologies/polyscan/issues/140); `AnalysisFailure` was not, because keeping partial results when an analyzer crashes on one file is graceful degradation, which FailFast rules out (see the `domain/analysis_coverage.go` row)
- **Deps captured-syntax chain** (`5183798`, `e6ed093`, `82ac00a`, `20b6eff`, `fc7c247`, `fe28bda`, `1becbc7`, `5fb4255`, `8d91d0d`, `b898298`, `64dc6ba`, `4d0b0a6`) — pyscn's module analyzer moved from a filesystem-backed resolver to the in-memory parsed graph and patched the regressions that move introduced (stdlib exclusion, sibling imports, exact absolute imports, namespace imports, re-exports); plus `.pyi` stubs, project-root anchoring, a `DependencyGraph.Clone()` deep-copy fix and waived empty graphs for `check`. jscan's `ModuleAnalyzer` has always taken parsed ASTs and never read disk, has no `Clone()`, and `module_analyzer.go` is reference-only
- **CBO from-import namespace grouping and dependency-kind bookkeeping** (`91353fa`/#751, `818d340`) — Python import-naming gaps; jscan's four independent extraction passes have neither. `fb61918` (show the type-hint breakdown for CBO 0 classes) needed no port: jscan's formatters gate nothing on `coupling_count > 0`
- **LCOM refinements** (`7858c94`, `6826b85`, `ea8524a`, `4338b2d`) — jscan computes no LCOM. Design direction for the Go/Rust cohesion analysis in `polyscan/internal/analysis/cohesion.go`: constructor exclusion (`4338b2d`) already holds there because receiverless functions are excluded; `@property` handling is Python-only; Protocol/Enum skipping is Python-only; **stub-method exclusion** (`6826b85`) applied on 2026-09-16 as [polyscan#139](https://github.com/ludo-technologies/polyscan/issues/139): a receiver method that touches no field and takes part in no sibling call is excluded
- **Bare `yield` keeps a function a generator** (`2c3d8ac`/#773) — Python-only; JS generator-ness is syntactic (`function*`)
- **Docstring size exclusion** (`8e75d21`), **Type-4 cap below Type-1** (`8de0b70`/#735), **explicit semantic fragments** (`18b500c`, `256eb24`) — docstrings and semantic similarity are unported
- **Clone service reuses the snapshot and honors its file scope** (`58378ca`, `38e716d`, `06db950`, `af174d0`, `cea3616`) — already in sync: `DetectClonesInSnapshot` validates `req.Paths` against the snapshot and fails fast; the "informational clone failure" classification is pyscn CLI plumbing
- **elif arms not extra nesting** (`59cf977`/#728) — already in sync via `isElseIf`
- **Complexity reporting cleanup and `check`/MCP gates over the complete population** (`80ac979`, `7bcc22f`, `c487411`, `f6ce1c3`) — jscan already has `FunctionsParsed == TotalFunctions`; the rest targets class scopes, `check` and MCP
- **snake_case keys** (`9d341ff`), **unclamped duplication percentage** (`a3e1a96`), **empty directory contract** (`b6e5e3c`) — already in sync
- **Output layer** (`17b6590`/#752 write every requested report, `60b19fe` LCOM4 in text/CSV, `46f1d4b` cohesion violations in the HTML architecture card, `13b1a6c`, `ee79c99`, `a3c190a` diagnostics wording, `c1f9e8f` YAML tags) — reference-only. Note for a human: jscan's `writeAnalyzeYAML` in `service/output_formatter.go` and `domain.AnalyzeResponse` / `ToAnalyzeResponse()` are dead code (no CLI format reaches them, and the wrapper structs carry no `yaml` tags), candidates for removal rather than repair
- **Project root / config discovery** (`8f407be`/#765, `4b4d04e`/#738, `e9e2d98`, `0090bf5`) — config-loader layer

Skipped during the 2026-09-27 sync (`7358893`..`7d2d90f`, v1.32.0..v1.32.1+19):

- **Dead-code reason from the CFG region** (`175de1f`/#471): **blocked on [polyscan#172](https://github.com/ludo-technologies/polyscan/issues/172)**. The port was written and works: jscan already labels the block after each terminator, and propagating that label through the rest of the dead region gives every block the right reason. On opencode it also turned 27 findings from `unreachable_branch` warnings into critical `unreachable_after_return` findings, and almost all of them were `finally` bodies. jscan's CFG does not route a `return`, `throw`, `break` or `continue` inside `try` through its `finally`, so the `finally` is reached only through the dead block after the terminator. Port `175de1f` once #172 is fixed. Expect one more gap then: a loop's closing `}` is not a CFG statement, so `MergeContiguousFindings` still splits a dead region at that line
- **Score duplication over the full clone population** (`9d01e4e`/#789): the bug is latent in jscan. It needs a narrowed `min_similarity`/`max_similarity`, and `polyscan analyze` always runs `DefaultCloneRequest`, whose range is 0–1. Porting it would add a `duplicated_fragments` JSON field for a filter no user can set. Port it if the similarity range ever becomes configurable
- **Default `complete_linkage` grouping** (`314d32e`): jscan still defaults to `connected` (`analyzer.DefaultCloneDetectorConfig`). The switch changes clone groups and duplication scores, so it is a product decision, not a sync port. The config-key validation half is config-loader-layer work
- **Identical short bodies and single-statement fragments** (`d1a0175`, `754c417`, `63bd396`/#794, #814): this is a new feature. Undersized fragments are reported when an identical twin exists, and single-statement fragments are reported only as Type-1. The three commits only work together, and the single-statement check needs pyscn's Python node kinds remapped to JS/TS. Not ported this run
- **k-core exclusions in the uncovered-pair fallback** (`df88915`): jscan never had the #797 uncovered-pair fallback
- **`min_lines` in SLOC, default 8** (`dc65211`/#803), **raw-metrics def header** (`22c3d69`): `raw_metrics.go` is unported
- **Type-4 semantic overlap** (`2d1e3e0`, `9a35a16`/#792): semantic similarity is unported
- **Dead-code summary over the full population** (`03266ed`/#791): this is already in sync, because `aggregateDeadCodeScans` counts every scanned file. Note for a human: `DeadCodeServiceImpl.generateSummary` in `service/dead_code_service.go` still sums over the filtered files, but only its own tests call it, so it is dead code rather than a live bug
- **File patterns relative to the project root** (`74aae4d`): this is already in sync, because `CollectJSFiles` matches paths relative to the walked root
- **Default exclusion of `examples`, `docs`, `docs_src`, `demo` and `samples`** (`755c47a`/#809): this is a product decision and was not ported. jscan's default excludes have none of these directories
- **Project-root discovery** (`aa75a63`, `8851a81`): this is config-loader-layer work
- **Community score rework** (`6ba7cd6`, `96a879c`): community detection is unported
- **LCOM method references** (`5ad3beb`/#678): jscan computes no LCOM. The Go cohesion analysis in `polyscan/internal/analysis/cohesion.go` had the same bug with method values and was fixed in this run's PR. Rust does not have it, because `self.value` without a call is always a field

Core issues raised by the 2026-09-27 run:

- **Group splitting along the retained pairs** belongs in `core/clone` next to `FilterGroupsWithoutBackingPairs` ([polyscan#173](https://github.com/ludo-technologies/polyscan/issues/173)); pyscn has two copies of it and jscan one

Core issues raised by the 2026-08-17 run (both resolved):

- ~~**pyscn's dependency topology duplicates `core/graph.ChainFinder`.**~~ Resolved: `core/graph.ChainFinder` gained context cancellation and globally ranked top-N chains ([polyscan#59](https://github.com/ludo-technologies/polyscan/pull/59), tag `core/v0.2.3`), and pyscn adopted it in `693c8ac`, deleting `dependency_chains.go` and reducing `dependency_topology.go` to a facade. pyscn's `MaxDepth` now counts the edges of its top-ranked chain, matching jscan
- ~~**`calculateParseErrorPenalty` is language-independent scoring policy.**~~ Resolved: moved to `coredomain.ParseErrorPenalty` ([polyscan#61](https://github.com/ludo-technologies/polyscan/pull/61), tag `core/v0.2.4`), called by jscan in the same PR and by pyscn in `eb372cb`. Both keep the local bounds as aliases; the formula is unchanged, so no score moves

## Sync history

| Date | pyscn SHA | Summary |
|---|---|---|
| 2026-06-11 | `f0457d7` | Set the baseline to the state jscan's initial implementation was based on (v1.4.1). The ~210 commits since then are the unported backlog targeted by the initial catch-up |
| 2026-06-11 | `73acc60` | Initial catch-up: clone detection area done. APTED correctness fixes (ascending key roots, forest-distance subtreeCost, max(size) normalization), bounded large-tree approximation (same-shape distance, label/shape profiles), Jaccard pre-filter, Type-1 textual-match gate and Type-2 syntactic gate (ported textual/syntactic similarity), threshold recalibration (0.85/0.75/0.70/0.65), Type-3 disabled by default, overlapping-range pair rejection (isOverlappingLocation), strict-subset group member removal (group_dedup), complete_linkage rewritten as agglomerative clustering, MinLines/MinNodes 10/20, LSH auto-enable by estimated pair count, introduced `parser.OrderedChildren` |
| 2026-06-11 | `8650522` | Initial catch-up: scoring area done. Duplication metric switched to K-Core group density (groups per 1000 lines × 20, capped at 10%) with 0–10% penalty scale (`0886cfb`, `760bc87`), CBO coupling calibration softened (medium weight 0.3, saturation 0.40, `333c9ac`, `9c84a3d`), architecture score now uses compliance directly (`3c9927c`), `WeightedViolations` exposed (`e6b9920`), module-scope label `__main__` → `<module>` via `domain.ModuleFunctionName` (`2cc013c`) |
| 2026-06-12 | `fb3fe92` | Initial catch-up: misc area done (catch-up complete; baseline moved to `fb3fe92`). LSH index switched to int fragment IDs with `WithMaxCandidates` cap (default 1024) and sorted deterministic candidates, reachability `allSuccessorsReturn` memoized (removed `copyVisited`), lazy-import cycle exclusion ported as dynamic-`import()` edge skipping in circular detection (#460), coupling zone classification aligned (Zone of Pain requires D≥0.5/Ca≥2/I≤0.3/A≤0.3, main sequence D≤0.2, instability defaults 0.3/0.7), dead-code findings merged per contiguous same-reason region with EndLine sort tiebreak, bare-`;` (empty statement) dead-code noise filtered (`empty_statement` now maps to `NodeEmptyStatement`), domain error codes TIMEOUT/CANCELLED/NOT_IMPLEMENTED/INTERNAL added |
| 2026-07-11 | `3c6f8a8` (targeted) | Targeted port of the Agent Skills mechanism (`.claude-plugin/`): marketplace + skills-only plugin manifest and the 4 CLI-first skills (health-check, cli-analysis, refactoring, architecture-review) adapted to jscan's CLI; README gains an "AI Agent Integration" section (`uvx add-skills ludo-technologies/jscan`, Claude Code plugin install). Baseline SHA intentionally left at `249b121` — mapped algorithm files were not re-synced |
| 2026-07-08 | `249b121` | Normal sync (`fb3fe92`..`249b121`, 18 commits touching mapped files). Fragment hash now computed (FNV-64a hex of Type-1 normalized content) and forwarded to clone output (`012cd15`, issue #488); clone-group classification now prefers the highest-similarity pair's type over majority vote, fixing high-similarity pairs being hidden behind Type-3 transitive edges and dropped by the default type filter (`d9dcada`, issue #525); added `dedupeCoveredGroups` post-grouping pass to suppress whole groups covered by another group's larger overlapping windows (`e2a0b1b`, issue #518) and `filterCloneGroupsWithoutBackingPairs` to drop groups left with no positive-similarity backing pair (`10d116d`); duplication metric switched from K-Core group density to fragment ratio (clonedFragments/totalFragments × 100, 0–30% scale replacing 0–10%) (`4faf98f`, `b58cfbd`). Skipped: internal detector-result-type refactor (no behavior change), 4 CBO commits (Python-scoping/import-specific, `cbo.go` is reference-only), cognitive-metrics risk scoring (depends on unported CognitiveComplexity), and the new module-community-detection feature (6 commits, entirely new subsystem) — see "Pending changes" |
| 2026-07-12 | *(monorepo `core/`)* | Phase 2b clone adoption (#9 / `c57f3b4`): jscan deleted local `grouping_strategy.go`, `textual_similarity.go`, `syntactic_similarity.go`, `ast_features.go` and wired `core/clone` (grouping strategies, dedup passes, similarity gates, `PairClassifier`). Language-side keeps fragment extraction, TreeConverter, JS cost model, LSH orchestration, and `removeJSComments` as `CommentStripper`. Further algorithm changes for clone grouping/gates go to `core/clone`, not jscan copies |
| 2026-07-18 | *(monorepo `core/`)* | Phase 2b CFG/graph/LSH adoption (#11): jscan adopted `core/cfg` data structures, reachability, complexity, dead-code post-processing; `core/graph` Tarjan SCC and Martin metrics; `core/lsh` MinHash/index kernels; and shared `core/domain` scoring calculators. JavaScript classifiers, CFG construction, source enrichment, dynamic-import filtering, LSH candidate caps, and jscan-specific scoring penalties remain language-side. Intentional core semantics: exception edges contribute to complexity, unreachable registered blocks are included in complexity, and isolated dependency nodes use I=0/D=1. Reachability prunes only normal fallthrough after terminators so exception/control-transfer targets remain reachable |
| 2026-07-18 | *(monorepo `core/`)* | Phase 2b APTED adoption (#10): jscan deleted its local APTED algorithm/tree/default-cost copies and now uses `core/apted` directly with max-size normalization. Language-side keeps only the JS/TS parser converter and JavaScript cost model. |
| 2026-07-25 | `6af3ee7` (v1.27.0) | Normal sync (`249b121`..`6af3ee7`, 33 commits touching mapped paths; first run of the recreated `/sync-pyscn` command). Ported: function-coverage disclosure (#597, `92c436d`+`ca95d2e`) — `FunctionsParsed` (pre-`min_complexity`-filter parsed count) added to `ComplexitySummary`/`AnalyzeSummary`, propagated through the complexity service and analyze summary, surfaced as `functions_parsed` in JSON and as "N reported / M parsed" in text and HTML output when filtering drops functions. Already in sync via core adoption: LSH `FindCandidatesLimit` cap delegation (`a3854e5` ≙ polyscan#20) and lazy-predecessor-edge filtering (`e5b2482` ≙ jscan's `loadTimeGraph.Predecessors`). 11 pyscn core-adoption migrate commits carry nothing to port. Skipped: 7 CBO Python-scoping refinements (reference-only), config-loader plumbing (`6a12c8b`, `f1e9a4b`, `a70b5a5`), LCOM test — see "Pending changes" |
| 2026-07-28 | `81b7ec0` (v1.29.0) | Normal sync (`6af3ee7`..`81b7ec0`, 76 commits, 22 touching mapped paths). Ported: `FunctionsParsed` now excludes functions dropped by `report_unchanged` (pyscn `e9f9e6f`, a follow-up to the #597 disclosure ported last run) — the count moves inside `filterFunctions`, after the report-unchanged check and before the min/max complexity filters, so the "N reported / M parsed" line no longer blames `min_complexity` for report-unchanged drops. Verified by unit tests plus an E2E run on a 3-function fixture: `1 / 3` before, `1 / 1` after with `report_unchanged: false`, `3 / 3` with it on. Nothing this run belongs in `core/` — the new pyscn rollups are presentation-layer joins over language-side results, not shared algorithm. Skipped: 5 architecture-suggestion commits (unported `domain/suggestion.go`), the module + directory quality rollup subsystem (~20 files, [polyscan#31](https://github.com/ludo-technologies/polyscan/issues/31)), MCP registry listing — see "Pending changes" |
| 2026-08-14 | `1ab776e` | Normal sync (`81b7ec0`..`1ab776e`, 68 commits, 31 non-merge touching mapped paths). Ported three changes. (1) **Complexity aggregates over the analyzed population** (pyscn `8b2744f`/#696 + `a17e859`, settled by `d4592ec`): the summary and the directory rollups were built from the filtered function list, so raising `--min-complexity` removed the high-complexity functions it was meant to surface from the population the health score divides by, and the score moved with it. Both now read the unfiltered population, and `TotalFunctions` = `FunctionsParsed` = that population — which retires the "N reported / M parsed" disclosure ported in the previous two syncs, in text and HTML alike. (2) **Parse-error gate** (pyscn `c28c656` + `b0fd273`/#690): jscan's parser now rejects trees carrying ERROR or MISSING nodes (tree-sitter always returns a tree, so a broken file used to be measured on whatever the grammar salvaged), `check` fails on unanalyzable files with `--allow-parse-errors` as the escape hatch, unknown flags and bad `--select` values exit 2 instead of reading as a quality verdict, and the health score charges a floored penalty for the unanalyzed fraction. `ComplexitySummary` gains `TotalFiles`/`SkippedFiles`, and CLI, text and HTML output name the skipped files. E2E: a 3-file fixture with one broken file scores 95/A before and 77/B after, `check` exits 2, `--allow-parse-errors` exits 0. (3) **Load-time dependency depth** (pyscn `29115cb`): max depth and the longest chains now read the same load-time graph as cycle detection, via the newly exported `analyzer.LoadTimeGraph`. Skipped: per-function SLOC (#551, depends on unported `raw_metrics.go`), cognitive-complexity scope boundaries, config discovery, and the 20-commit topology subsystem that duplicates `core/graph.ChainFinder` — see "Pending changes" and the core issues recorded there ([polyscan#56](https://github.com/ludo-technologies/polyscan/issues/56), [polyscan#57](https://github.com/ludo-technologies/polyscan/issues/57), [polyscan#58](https://github.com/ludo-technologies/polyscan/issues/58)) |
| 2026-08-17 | `f4f569c` (v1.29.1+10) | Normal sync (`1ab776e`..`f4f569c`, 10 commits, 3 touching mapped paths). Nothing ported: the two migrate commits are pyscn adopting `core/graph.ChainFinder` ([polyscan#56](https://github.com/ludo-technologies/polyscan/issues/56)) and `coredomain.ParseErrorPenalty` ([polyscan#57](https://github.com/ludo-technologies/polyscan/issues/57)), both of which land jscan's existing behavior on pyscn's side — including the MaxDepth-counts-the-ranked-chain's-edges semantics jscan has had since polyscan#59 — and the third is a Japanese-comment translation with no jscan counterpart. Deterministic class-ranking tie-breaks (`74f2512`) were already in jscan. Skipped: gating over the analyzed population (`64a6876`, blocked on the unported per-function SLOC gate) and the analyze HTML report redesign (#717, reported instead as [polyscan#65](https://github.com/ludo-technologies/polyscan/issues/65)) — see "Pending changes" |
| 2026-09-15 | `7358893` (v1.32.0) | Normal sync (`f4f569c`..`7358893`, 188 commits, 130 touching mapped paths, triaged by theme). Ported four changes. (1) **Type annotations are breakdown-only in CBO** (pyscn `1272336`/#757, JS/TS-native): the names a file's type annotations reference show in `type_hint_dependencies` but stay out of `coupling_count`, `dependent_classes` and the risk level, since TypeScript erases them. Found while porting: jscan's extraction matched `parser.NodeTypeAnnotation`, a node type the AST builder never emits (annotations keep the raw `type_annotation` name, like `new_expression`), so the breakdown had always been 0 and the coupling count never included annotations; the extraction now fires. (2) **`schema_version`** (`00d7f8c`/#750): top-level integer `1` on the analyze JSON document, `domain.AnalyzeSchemaVersion`, set in `newAnalyzeResponseJSON`. (3) **Empty collections serialize as `[]`** (`f05ec90`/#747): wider in jscan than in pyscn — `dead_code.files`, `most_coupled_classes`, `base_classes`, the cycle and coupling-zone lists, `longest_chains`, and each module's `direct_dependencies`/`dependents`/`public_interface` were `null`; `DependencyGraph.Successors`/`Predecessors` never return nil now, `public_interface` is filled from the module's deduplicated exports, and the never-populated `most_depended_upon_classes` key was dropped. A CLI test walks the analyze JSON of the Go, JavaScript and a declaration-only TypeScript fixture and fails on any `null`. (4) **No hard failure on a parsed population without functions** (`62a71f4` + `00fb1da`): a directory of type declarations dropped the whole complexity section with "no functions found to analyze"; it is now an empty result, and an all-skipped selection returns its skip counts instead of an error, matching the dead code and coupling services. Nothing belongs in `core/`. Skipped: the class-scope feature (34 commits), the snapshot restructuring (36), typed diagnostics ([polyscan#140](https://github.com/ludo-technologies/polyscan/issues/140)), the deps captured-syntax chain, CBO/LCOM Python refinements (Go/Rust stub-method exclusion as [polyscan#139](https://github.com/ludo-technologies/polyscan/issues/139)) — see "Pending changes" |
| 2026-09-27 | `7d2d90f` (v1.32.1+19) | Normal sync (`7358893`..`7d2d90f`, 31 commits, 21 touching mapped paths). Ported five changes. (1) **Re-split clone groups after member suppression** (`69f577b`/#798): the detector validated groups before the suppressed members' pairs were dropped. (2) **Align clone groups with the type-filtered pairs** (`cf077e1`, `fc35b60`/#800): the default type filter drops Type-3 pairs but kept each group whole, so a group could hold a member only a Type-3 pair connected. Both use one new helper, `analyzer.SplitGroupsByPairs`. On opencode the clone count went from 1381 to 1368 and the groups from 355 to 359, with the pair count unchanged; jpyc-lens went from health 86 to 87. (3) **CBO summary over the full class population** (`d54b2ed`): latent, because `polyscan analyze` sets no CBO display filter. (4) **Dead try reported from its header** (`f47d18d`/#808). An empty try body is covered too: the first statement of its catch, or else of its finally, marks the try. (5) **Go cohesion: a method value links like a call** (`5ad3beb`/#678, applied to `polyscan/internal/analysis/cohesion.go`, since jscan has no LCOM). Only Go opts in, through `Language.MethodValues`: Rust lets a field and a method share a name, and `self.value` is always the field. Held back: the dead-code region reason (`175de1f`), blocked on the `finally` routing false positive [polyscan#172](https://github.com/ludo-technologies/polyscan/issues/172). Group splitting belongs in core ([polyscan#173](https://github.com/ludo-technologies/polyscan/issues/173)). See "Pending changes" for the rest |
| 2026-07-24 | *(pyscn Phase 3 done, `152d9d0`)* | pyscn completed [polyscan#12](https://github.com/ludo-technologies/polyscan/issues/12): adopted `core/clone` (#664), `core/apted` (#669), `core/cfg` (#670), `core/graph` (#672), `core/lsh`+`core/lcom`+`core/dfa`+`core/semantic` (#673), and `core/domain` scoring/grade mapping (#674). Every **sync**-classified row in this file is retired: rows whose files were deleted on both sides (`apted.go`, `minhash.go`, `ast_features.go`, `grouping_strategy.go`/`*_grouping.go`/`group_dedup.go`, `syntactic_similarity.go`) were removed outright; rows whose files remain as thin core-delegating wrappers on both sides (`lsh_index.go`, `cfg.go`, `reachability.go`, `coupling_metrics.go`, `circular_detector.go`, `domain/analyze.go`, `domain/system_analysis.go`) were reclassified to **case-by-case**. Grade computation is identical between pyscn and jscan by construction — both call `core/domain.GradeFromScore`/`IsHealthyScore` directly and each has its own unit test asserting parity with core |
